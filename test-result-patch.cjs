const fs = require('fs');
let code = fs.readFileSync('src/pages/TestResult.tsx', 'utf-8');

// I will just use regex to replace the UI and add the stats computation.
// Wait, actually I'll just write a whole new script.
